# Analyses Scripts

This directory contains scripts that were used for the analyses in the paper. It also includes the scripts to download data from BigQuery and the GitHub Rest API (tracking progress in a DB for resumability and using multiple tokens for more parallelization) – which was a major effort :)
